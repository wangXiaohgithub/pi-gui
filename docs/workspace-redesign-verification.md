# Workspace redesign verification — September 22, 2026

**P0.3/P0.4 review and captured turns, plus P1.1/P1.2 custom extension views, passed their final gates within the macOS scope below.** The earlier Pi upgrade and workspace foundation evidence remains in the [implementation plan](workspace-redesign-plan.md). Evidence links point to retained local `.artifacts` files; these generated artifacts are not published with the docs.

Implementation checkpoints: `fef2a61ea` (helper/hooks/examples), `a67b39af1` (review storage), and `dc0fbc13a` (desktop integration/tests). The [machine-readable final result](../.artifacts/redesign-final-cyo52b/result.json) records the coordinated verification outcome.

| Evidence level                 | Result and scope                                                                                                                                                                                                                                                                                           | Retained evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Repository baseline            | `pnpm check` passed formatting, lint, architecture and type checks; 103 guard, 28 catalog, 86 driver, 9 helper, 17 example, 27 release-helper and 216 desktop unit tests passed. One invalid-filename fixture was skipped because APFS cannot create the Linux-only input.                                 | [Complete check log](../.artifacts/redesign-final-cyo52b/check.log)                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Fixture-backed Core Electron   | **27 distinct cases** passed across the focused runs: 26 in the review/workbench/extension summary plus the final Stop-during-draft case. The final four-case rerun passed Stop/draft protection and all three security/window cases. The PR example was also rerun after its hover-contrast fix.          | [26-case summary](../.artifacts/review-redesign/verification-summary.md), [final four cases](../.artifacts/redesign-final-cyo52b/core-last.log), [PR rerun and packaged cases](../.artifacts/redesign-final-cyo52b/packaged-and-hover.log)                                                                                                                                                                                                                                                                                              |
| Real-provider conversation     | `openai-codex/gpt-5.6-sol`: all **10 checkpoints**, no assertion failures. Sending/streaming, reading, tools, switching, drafts, archive/restore and restart passed. Stop aborted an actual 25-second tool in **568 ms**; its completion file was absent.                                                  | [Conversation result](../.artifacts/verify-pi-gui/run-bif5h7/result.json), [Stop proof](../.artifacts/verify-pi-gui/run-bif5h7/stop-proof.json), [trace](../.artifacts/verify-pi-gui/run-bif5h7/conversation.zip)                                                                                                                                                                                                                                                                                                                       |
| Real-provider maintenance      | Same provider/model: all **7 checkpoints**, no assertion failures, covering skills/aliases, pinning, thread-list navigation, a Git-checked worktree and queued follow-up/steering.                                                                                                                         | [Maintenance result](../.artifacts/verify-pi-gui/run-XBuK0l/result.json), [follow-up trace](../.artifacts/verify-pi-gui/run-XBuK0l/follow-ups.zip)                                                                                                                                                                                                                                                                                                                                                                                      |
| Real-provider PR extension     | **1 passed** using the actual model, Pi extension/tools and local Git. GitHub metadata was an offline fixture; no GitHub review was posted. Recorded findings, file navigation and the unsent fix draft were inspected.                                                                                    | [Run log](../.artifacts/redesign-final-cyo52b/live-extension.log), [findings](../.artifacts/redesign-final-cyo52b/live-extension/live-extension-pr-review-a-e6745--and-prepares-an-unsent-fix/real-provider-pr-findings.png), [unsent fix](../.artifacts/redesign-final-cyo52b/live-extension/live-extension-pr-review-a-e6745--and-prepares-an-unsent-fix/real-provider-unsent-fix.png), [trace](../.artifacts/redesign-final-cyo52b/live-extension/live-extension-pr-review-a-e6745--and-prepares-an-unsent-fix/extension-review.zip) |
| Local macOS arm64 package      | Build, **2 packaged UI cases**, model registry, dependency imports and native-dependency guards passed. The frame bridge loaded from ASAR; Chord hydrated at 0, incremented to 1 and retained 1 after view reload. The app launched and created a task.                                                    | [Package build](../.artifacts/redesign-final-cyo52b/package.log), [UI cases](../.artifacts/redesign-final-cyo52b/packaged-and-hover.log), [runtime guards](../.artifacts/redesign-final-cyo52b/packaged-runtime.log)                                                                                                                                                                                                                                                                                                                    |
| External local author packages | Helper and both example tarballs installed outside this repository and loaded through actual Pi 0.87.0 with zero loader errors. Commands/tools, source identity and browser assets were present. This proves loading/registration, not provider execution or Electron rendering by that standalone loader. | [Authoring proof](../.artifacts/extension-authoring/README.md), [structured result](../.artifacts/extension-authoring/proof-result.json)                                                                                                                                                                                                                                                                                                                                                                                                |

The coordinating agent opened the actual conversation, follow-up and PR traces in the trace viewer: no errors were shown. Final screenshots were inspected, and all owned Electron processes exited. Passing reruns follow repaired native transcript-ID association, outgoing-draft preservation and Stop availability, stale activation/replacement handling, shared-package build ordering/dev watch, and PR hover contrast. Independent source reviews closed without outstanding findings.

Earlier unsuccessful runs remain retained: [run-VaOHQ6](../.artifacts/verify-pi-gui/run-VaOHQ6/result.json) was blocked at sandboxed Electron launch; [run-uEJQVA](../.artifacts/verify-pi-gui/run-uEJQVA/result.json) finished its model response before the recipe could press Stop. The verification skill was corrected to require an observed active tool, and `run-bif5h7` then proved actual cancellation. These earlier runs are not counted as passes.

The package used an ad-hoc signature. It was **not notarized, published, or installed into `/Applications`**; Windows/Linux desktop behavior was not tested. The helper remains private/local, and P2 is a separate scope. Capture defaults remain 2 seconds, 10,000 paths, 64 MiB total and 16 MiB per file, with explicit unavailable/partial coverage as documented in the [plan](workspace-redesign-plan.md). Captures record interval changes, not exclusive authorship. Browser isolation does not sandbox trusted extension backend code. Pi's `sendUserMessage` returns `void`: rejected provider admission may leave PR Review Requested until reload restores Interrupted; no unknown-outcome request is automatically replayed.

## Follow-up: Pi 0.87.1 and compact, resizable workspace

The September 22 follow-up upgrades Coding Agent/Chord to 0.87.1, including GPT-6 Sol/Luna. Provider SDK pins match the new Pi dependencies. Runtime and packaged guards require both models for OpenAI, OpenAI Codex, and GitHub Copilot. Existing enabled-model preferences are preserved: a newly bundled model still needs enabling in Settings → Models when the user's list is restricted.

The workspace now has one saved width across tools, the chooser, and tasks; pointer dragging and keyboard arrows/Home/End adjust it. Responsive bounds keep the layout usable as window size changes. The hide icon sits next to +; the collapsed panel can be reopened from the header. Workspace and task title occupy one header row. Settings, Skills, Extensions, and Scheduled retain their existing page layouts and shared visual tokens.

- `pnpm check` passed: [baseline log](../.artifacts/layout-polish-check-final.log), including 216 desktop unit passes and one platform skip. Final modified verification scripts also passed lint/typecheck.
- Seven [workspace Electron cases](../.artifacts/layout-polish-core-final.log) passed, including pointer and keyboard resizing, all tools/chooser retaining width, hide/reopen, task changes, narrow windows, restart, active-tab visibility, and GPT-6 Sol selection with reasoning shown. Four composer/sidebar cases passed in the [initial sweep](../.artifacts/layout-polish-core.log). That sweep's narrow-window failure was fixed and covered by the final run.
- The separate [macOS preview package](../apps/desktop/release-layout-polish/mac-arm64/pi-gui.app) passed [dependency/model/import checks](../.artifacts/layout-polish-packaged-runtime.log) and [two packaged UI cases](../.artifacts/layout-polish-packaged-ui.log): task creation and live Chord frame state. It is ad-hoc signed, not notarized or published; the earlier running preview bundle was preserved.
- Real `openai-codex/gpt-6-sol` passed streaming, reading during growth, tool output/file effects, switching during a run, Stop (220 ms), draft isolation, archive/restore, and transcript restoration. The [full recipe failed](../.artifacts/verify-pi-gui/run-SL1Gxi/result.json): Bravo's draft was `Unsent draft for Bravo` before close but `Unsent draft for Bravopdat` after restart. The [input log](../.artifacts/verify-pi-gui/run-SL1Gxi/composer-inputs.json) records a Meta key event in the restarted foreground window but does not capture the source of the four extra characters. Do not count this as a full real-provider restart pass or assume a root cause. The restart trace was opened and inspected and confirms that mismatch. Both owned Electron processes exited.
- An earlier [real-provider attempt](../.artifacts/verify-pi-gui/run-q7dgh6/result.json) stopped at archive because shortcut badges hid archive controls. Both unsuccessful attempts are retained. Controlled Electron draft/layout restart tests pass; the foreground live draft discrepancy still needs an isolated follow-up.

Independent source review found one issue (newly selected overflow tabs were offscreen); it was fixed and rechecked. All changes remain local; no PR/hosted CI or release publication was performed in this follow-up.

## Follow-up: generated artifacts and large Changes lists

The reported 2,000-file partial comparison included local app backups and verification evidence. Git listed 57,922 status entries before this fix. Root-anchored ignores for `.artifacts` and `.pnpm-store` reduced the installed app's review to six real changed files at verification time; all artifacts were retained. A Git regression test verifies ignored generated files disappear while ordinary untracked source and explicitly tracked files in ignored directories remain reviewable.

Coverage explanations now use a collapsed “Comparison has limits” disclosure. Large lists use stable single-line rows and Chromium's off-screen content rendering optimization; full filenames remain available on hover. Review bounds and partial-coverage semantics are unchanged.

- The [focused initial run](../.artifacts/review-lag-core.log) passed 24 cases; its new large-list case failed on an immediate controlled-checkbox assertion. The [scope rerun](../.artifacts/review-lag-scopes-final.log) passed six cases but its nested accessibility locator again timed out. Diagnostic evidence showed the checkbox actually checked. After using the existing direct test-ID pattern, the [2,000-file Electron case passed](../.artifacts/review-lag-large-final.log), covering wheel scrolling, expandable notes, off-screen keyboard selection, diff content, and marking reviewed. Across these runs, all 25 distinct focused cases passed. Scroll samples are diagnostic only; no before/after frame-rate benchmark was established.
- Build, modified-file lint, desktop typecheck, independent source review, [packaged runtime checks](../.artifacts/review-lag-packaged-runtime.log), and macOS signature validation passed. Review corrected the notice wording because attribute-check limits do not always omit changes.
- Installed the ad-hoc signed build into `/Applications/pi-gui.app`; ASAR SHA-256 `f615e0dfac5175e65081ad0e9edda81474c43760c1117b1b7deaac78164126ab` matched the built package. The previous app remains in `.artifacts/review-lag-install-20260922-170257/previous-pi-gui.app`. The actual installed app reopened the existing task, showed six changed files without the partial-comparison notice, and opened the `.gitignore` diff. No conversation was sent during verification.

This follow-up remains local. No publication, remote push, or hosted CI run was performed.
